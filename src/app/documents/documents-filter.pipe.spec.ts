import { DocumentsFilterPipe } from './documents-filter.pipe';

describe('DocumentsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
